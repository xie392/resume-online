export enum InputType {
  Text = "text",
  Select = "select",
  Date = "date",
  Editable = "editable",
  Image = "image",
}

export enum Nav {
  BaseInfo = "baseInfo",
  EducationExp = "educationExp",
  WorkExp = "workExp",
  ProjectExp = "projectExp",
  Advantage = "advantage",
}

export enum AvatarType {
  Circle = "circle",
  Squre = "square",
}

export enum Align {
  Left = "left",
  Center = "center",
  Right = "right",
}

export enum Layout {
  // 传统布局
  Classical = "classical",
  // 左右布局
  LeftRight = "leftRight",
}
